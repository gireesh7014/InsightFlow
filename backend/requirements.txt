# InsightFlow Backend Dependencies
# ================================

# Web framework — FastAPI is async-first and auto-generates API docs
fastapi==0.115.0
uvicorn==0.30.6          # ASGI server to run FastAPI

# Data processing
pandas==2.2.2            # DataFrame operations, CSV parsing
numpy==1.26.4            # Numerical computing (pandas depends on this)
scipy==1.14.1            # Statistical tests (skewness, correlation significance)

# Visualization (server-side chart generation)
matplotlib==3.9.2        # Core plotting library
seaborn==0.13.2          # Statistical visualization (built on matplotlib)

# Database
sqlalchemy==2.0.35       # ORM — maps Python classes to database tables

# File handling
python-multipart==0.0.9  # Required by FastAPI for file upload (UploadFile)

# Machine Learning & Statistics (Week 2+)
scikit-learn==1.5.2      # Isolation Forest, ML utilities
statsmodels==0.14.2      # OLS regression, statistical tests, STL decomposition

# LangGraph Pipeline (Week 2)
langgraph==0.2.60        # State machine for multi-node AI pipelines
langchain-core==0.3.25   # Core abstractions (required by langgraph)

# LLM — Google Gemini (free tier: 15 req/min)
google-generativeai==0.8.3  # Gemini SDK for query understanding + synthesis

# Utilities
pydantic==2.9.2          # Data validation (FastAPI uses this internally too)
python-dotenv==1.0.1     # Load API keys from .env file
sse-starlette==2.1.3     # Server-Sent Events for streaming responses
